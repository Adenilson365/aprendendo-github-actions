name: CI/CD Fake
on: 
    workflow_dispatch:  
jobs:
    CI:
        runs-on: ubuntu-latest
        environment: prd
        steps:
            - name: DeployFake
              run: echo "Deploy no ambiente ${{ vars.ENV }}"


