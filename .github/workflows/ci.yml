name: CI/CD Fake
on: 
  push:
jobs:
    CI:
        runs-on: ubuntu-latest
        steps:
          - name: teste
            run: |
              # echo "${{ github.ref_name == 'main' && 'prod' || (github.ref_name == 'dev' && 'dev' || 'feat') }}"
              # echo "${{ github.ref_name == 'main' && 'prod' || (github.ref_name == 'dev' && 'dev' || 'feat') }}"
              echo "${{ github.ref_name == 'main' && 'prod' || github.ref_name == 'dev' && 'dev' || 'feat' }}"