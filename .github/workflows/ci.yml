name: CI/CD Fake
on: 
    pull_request:  
jobs:
    CI:
        runs-on: ubuntu-latest
        permissions:
          pull-requests: write
          issues: write
        steps:
            - name: Checkout do código
              uses: actions/checkout@v4
            - name: Comentar PR
              uses: actions/github-script@v7
              with:
                  script: |
                    const { execSync } = require('child_process');

                    function runCommand(command) {
                      try {
                        return execSync(command, { encoding: 'utf-8' }).trim();
                      } catch {
                        return '';
                      }
                    }


                    const planDetails = runCommand('cat plan.txt | grep "Plan:"') || "Nenhuma informação encontrada";
                    
                    const destroyResources = runCommand('cat plan.txt | grep "will be destroyed"') || "Nenhum recurso será destruído";
                    const modifyResources = runCommand('cat plan.txt | grep "will be changed"') || "Nenhum recurso será alterado";
                    const createResources = runCommand('cat plan.txt | grep "will be created"') || "Nenhum recurso será criado";


                    const commentBody = `👋 Thanks for reporting!\n
                    <h1>Resumo do Plano:</h1><br>
                    <span style="color: red;">${planDetails}</span><br>
                    "=====================================================================================";
                    <h1>Recursos a serem destruídos</h1><br>
                    <p>${destroyResources}</p><br>
                    "=====================================================================================";
                    <h1>Recursos a serem modificados</h1><br>
                    <p>${modifyResources}</p><br>
                    "=====================================================================================";
                    <h1>Recursos a serem criados</h1><br>
                    <p>${createResources}</p><br>`;

                    github.rest.issues.createComment({
                      issue_number: context.issue.number,
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      body: commentBody
                    });