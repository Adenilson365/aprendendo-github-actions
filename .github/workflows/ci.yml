name: CI/CD Fake

on:  
  workflow_call:
    secrets:
      ENV:
        required: true
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Debug branch name
        run: | 
          echo "${{ github.ref_name == 'main' && 'prod' || (github.ref_name == 'dev' && 'dev' || 'feat') }}"
          echo "Teste"
          echo "${{github.base_ref}}"
          echo "Secret"
          echo "${{secrets.ENV}}"

