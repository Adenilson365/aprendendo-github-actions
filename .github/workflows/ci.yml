name: CI/CD Fake

on:  
  workflow_call: 
env:
  ambiente: prod
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Debug branch name
        run: | 
         echo "${{ github.event_name == 'pull_request' && env.ambiente = github.base_ref || env.ambiente = github.ref_name }}"

         echo "${{env.ambiente}}"

